pipeline {
    agent { node { label 'agent' } }
    stages {
        stage('Build') {
            steps {
                echo "anil kumar"
            }
        }
        // stage('Test') {
        //     steps {
        //         //
        //     }
        // }
        // stage('Deploy') {
        //     steps {
        //         //
        //     }
        // }
    }
    post { 
        always { 
            echo 'I will always say Hello again!'
        }
    }
}